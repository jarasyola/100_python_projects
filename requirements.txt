# Basic Requirements
    # Enable virtual environments by running commands below
        # pip install virtualenv
        # python3 -m venv .env
        # source env/bin/activate

    # To install packages in requirements library run
        # pip install -r requirements.txt

# Project 1 - pdf to audio/mp3 file 
pyttsx3  # test to speech conversion library
PyPDF2

# To use pyttsx3 we have to install espeak and ffmpeg first - choose the right command for your OS
# If you get error below
# OSError: libespeak.so.1: cannot open shared object file: No such file or directory
# Install espeak
# https://stackoverflow.com/questions/65274843/oserror-libespeak-so-1-cannot-open-shared-object-file-no-such-file-or-directo

# Photo Editor
Pillow

#phone tracker
phonenumbers